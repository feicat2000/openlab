# ── Core ──────────────────────────────────────────────────────────────────────
pyyaml>=6.0.1
pydantic>=2.5.0
rich>=13.7.0
click>=8.1.7
python-dotenv>=1.0.0
loguru>=0.7.2

# ── Async / scheduling ────────────────────────────────────────────────────────
asyncio-mqtt>=0.16.2
apscheduler>=3.10.4

# ── REST API ──────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# ── Opentrons ─────────────────────────────────────────────────────────────────
# OT-2 on-robot: opentrons (pre-installed on robot)
# For simulation / remote HTTP control from workstation:
opentrons-http-api-client>=0.1.0
requests>=2.31.0

# ── Robotic arm (Universal Robots) ────────────────────────────────────────────
ur-rtde>=1.5.5           # RTDE real-time control for UR3e/UR5e/UR10e
# Alternative: pip install ur_rtde (same package, different index name)

# ── Serial / GPIO peripherals ─────────────────────────────────────────────────
pyserial>=3.5
RPi.GPIO>=0.7.1; platform_machine == "aarch64"   # Raspberry Pi only
gpiozero>=2.0; platform_machine == "aarch64"

# ── Safety & hardware monitoring ─────────────────────────────────────────────
psutil>=5.9.8

# ── LLM-assisted protocol parsing (optional) ─────────────────────────────────
anthropic>=0.23.0        # Claude API for natural-language protocol parsing
openai>=1.12.0           # fallback / GPT-4 option

# ── Data / reporting ──────────────────────────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.2
jinja2>=3.1.3
matplotlib>=3.8.3

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.4
pytest-cov>=4.1.0
python-multipart>=0.0.9
